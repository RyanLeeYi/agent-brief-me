# F35 evidence: Orca CLI entry points (brief-init Q0, brief-me Step 4 preflight, --no-orca)

This file is a skeleton. The sections below must be filled in by the user
personally, by actually walking through `brief-init`'s new Step 3 item 0 +
per-project Orca binding, and `brief-me`'s new Step 4 Orca preflight, against
a real Orca install - not fabricated or inferred from reading the skill text.

## 1. brief-init Q0: options and chosen value

Record the exact "Open dispatch windows inside Orca?" question as it
actually appeared (or the "Orca CLI not found..." fallback line if the
probe failed), and which value was picked.

## 2. Per-project Orca binding: options listed per project

For each tracked project, record the exact option list AskUserQuestion
showed ("Own repo" plus any "Under <displayName>" ancestors) and which
option was chosen for that project.

## 3. Headless dispatch (window=orca, watch=false) asks nothing

With `dispatch.window` set to `"orca"`, run brief-me and confirm a project
for headless (non-`--watch`) dispatch; record that Step 4 made no `orca
status` call and asked no extra question before dispatching.

## 4. `orca open` failure scenario

Trigger the "Orca is not running" -> "Open Orca" path with Orca unable to
start (or `orca open` itself failing); record the actual sequence of
questions shown and the final outcome.

## 5. `--no-orca` batch: where its windows actually opened

Dispatch a `--watch` batch that ends up passing `--no-orca` (either via the
CLI directly or via "Use console windows for this batch"); record where
each project's window actually opened (native console vs. Orca terminal).
