# F35 evidence: Orca CLI entry points (brief-init Q0, brief-me Step 4 preflight, --no-orca)

Machine-verified part (acceptance-verifier, 2026-09-01, HEAD b77574f): SKILL.md
text of both skills matches the acceptance clause by clause; `is_ancestor_workspace`
exercised with equal / trailing-slash / case-different paths; `--no-orca` smoke
against the real Orca CLI opened a native console window, wrote a `started`
record with `pid` and no `terminal`, and left `config.json` byte-identical;
`docs/schema.md` and README document `window`, `projects[].orca`, `--no-orca`.

User walkthrough (Ryan, 2026-09-01): walked brief-init and brief-me on the real
install after the plugin clone was updated to 2d6e0b1 and confirmed "F35 all
normal". Per-step detail below is the user's attestation; individual option
lists were not transcribed.

## 1. brief-init Q0: options and chosen value

Q0 appeared once the session picked up the updated skill (an earlier session
started before the clone was pulled did not show it - stale skill text, not a
probe failure). User confirmed the question and answer flow worked.

## 2. Per-project Orca binding: options listed per project

All seven tracked projects live under the registered `SideProject` folder
workspace, so each offered "Own repo" and "Under SideProject". User confirmed.

## 3. Headless dispatch (window=orca, watch=false) asks nothing

User confirmed no extra question in the headless path.

## 4. `orca open` failure scenario

Not exercised by the user (Orca was running throughout); the skill text for
this branch was verified by reading only.

## 5. `--no-orca` batch: where its windows actually opened

Machine smoke (see top): native console window, not an Orca tab.
