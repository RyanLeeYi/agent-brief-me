{"id": "F1", "status": "passing", "signed_off": true, "acceptance": ["docs/schema.md 以 JSON Schema 定義四種記錄型別，各附一個完整範例：question（id、project、title、body、choices[] 可選、recommendation 可選、severity 屬 {low,normal,high}、created_at）、report（id、project、summary、severity 可選、created_at）、status（ref＝某 question 或 report 的 id、status 屬 {answered,cancelled,read}、at）、answer（question_id、chosen 可選、free_text 可選、answered_at、consumed 布林）。所有 id 為 UUIDv4 字串，由寫入該記錄的工具產生，跨併發寫入者必須唯一。", "docs/schema.md 規定儲存方式：~/.agent-brief/inbox.jsonl 存 question、report、status 三種行；~/.agent-brief/answers.jsonl 存 answer 行；兩檔皆 append-only，既有行永不改寫。目前狀態由逐行摺疊推導：question 的狀態以引用其 id 的最後一筆 status 行為準（沒有 status 行即 pending）；report 被 read 型 status 行引用即為已讀；answer 的現值（含 consumed）以同 question_id 的最後一行為準。", "docs/schema.md 規定生命週期：question 由 pending 經 append 一筆 status 行轉為 answered 或 cancelled；任何記錄都沒有自動過期。", "docs/schema.md 規定原子附加規則：一筆記錄即一行、至多 8 KB，以 append 模式開檔並用單次 write 呼叫寫入。併發測試的 writer 實作的正是這條成文規則，因此測試紅了即代表 schema.md 寫的規則有問題。", "併發煙霧測試通過：tests/test_concurrent_append.py 啟動 5 個併發 writer，各依成文附加規則往同一個 inbox.jsonl append 20 行 question；結束後該檔恰含 100 行合法 JSON，且 100 個 id 互不重複。"], "touches": ["docs/schema.md", "tests/test_concurrent_append.py"], "requires": [], "name": "收件匣 schema 與協定文件"}
{"id": "F2", "status": "passing", "signed_off": true, "acceptance": ["執行 /brief-init 會建立 ~/.agent-brief/，內含 inbox.jsonl 與 answers.jsonl（建立時為空）及初始內容恰為 {\"projects\": [], \"collector\": null, \"notify\": null} 的 config.json。collector 與 notify 非 null 時皆為 argv 陣列；notify 執行時在陣列尾端依序附加 project 與 title 兩個參數。對既有安裝重跑不截斷、不覆寫既有資料，並明說略過。", "init 訪談使用者要納管的專案，逐項以 {\"name\": ..., \"path\": ...} 寫入 config.json 的 projects[]；path 先驗證存在才存。", "init 輸出 agent 側協定句（一段話：被派出的 session 在卡在使用者決策時、以及收工前，使用 brief-submit 投遞），並詢問是否 append 到使用者指定的 rules 檔；拒絕則不動任何檔案。", "init 執行冒煙檢查並逐步回報通過與否：確認 claude CLI 在 PATH、往 inbox.jsonl append 一筆測試 question、讀回、再依 schema.md append 一筆標記 cancelled 的 status 行；首次執行結束後 inbox.jsonl 恰含這兩行冒煙記錄。", "存在最小 plugin manifest，使本 repo 以本地 plugin 安裝後 /brief-init 可作為 skill 叫用；完整包裝收尾歸 F6。"], "touches": ["skills/brief-init/SKILL.md", ".claude-plugin/plugin.json"], "requires": ["F1"], "name": "brief-init skill：首次設定"}
{"id": "F3", "status": "passing", "signed_off": true, "acceptance": ["agent 以 question payload 呼叫 brief-submit 時，往 inbox.jsonl append 恰一行符合 schema 的 question；id 由 skill 產生（UUIDv4）並回報給呼叫者。缺 project 或 title、severity 值不合法、payload 自帶 id、或序列化後超過 8 KB 的 payload 一律拒絕並附理由，且不寫入任何東西；未帶 severity 時預設 normal。", "agent 以 report payload 呼叫時，append 恰一行符合 schema 的 report（id 由 skill 產生）。", "config.json 的 notify 為非 null argv 陣列時，投遞 severity high 的 question 會執行它恰一次，尾附 project 與 title 兩參數；low 與 normal 絕不執行；notify 為 null 時什麼都不跑。驗證方式：把 notify 指向一個把收到參數 append 進檔案的腳本，觀察恰有一行。", "本 skill 絕不阻塞等待答案，也絕不讀 answers.jsonl；投完即返回是它的全部契約。"], "touches": ["skills/brief-submit/SKILL.md"], "requires": ["F1", "F2"], "name": "brief-submit skill：agent 側投遞"}
{"id": "F4", "status": "passing", "signed_off": true, "acceptance": ["有未讀 report 時 /brief 先顯示它們，依 project 分組、各帶 summary 與距今時間；顯示時對每筆 report 往 inbox.jsonl append 一筆 read 型 status 行，下次 /brief 不再出現。", "/brief 依 project 分組、severity 由高至低走訪 pending question，逐題用 AskUserQuestion 呈現：有 recommendation 時它是第一個選項、其餘 choices 接續、永遠有一個 Skip 選項、自由文字永遠可填；choices 與 recommendation 皆無的題目只呈現 Skip 加自由文字。作答即往 answers.jsonl append 一行 answer、往 inbox.jsonl append 一筆 answered 型 status 行；Skip 不寫任何東西、題目維持 pending。", "config.json 的 collector 非 null 時，/brief 在讀收件匣前先執行它（argv 陣列，cwd 為 ~/.agent-brief）；非零 exit 即失敗，回報後 /brief 照常繼續。", "所有題目處理完後，/brief 列出擁有未 consumed answer 的專案並逐專案詢問是否派工；之後以確認過的專案名稱為參數叫用 python scripts/dispatch.py。F4 階段以一支會記錄自身 argv 的 stub dispatch 腳本驗收此條。", "收件匣為空時 /brief 明說收件匣是空的並結束，不叫用 dispatch。", "中途離開 /brief 不掉資料：未回答的題目維持 pending，下次 /brief 照常出現。"], "touches": ["skills/brief/SKILL.md"], "requires": ["F1", "F3"], "name": "/brief skill：晨報審核流程"}
{"id": "F5", "status": "passing", "signed_off": true, "acceptance": ["python scripts/dispatch.py <project> [<project> ...] 對每個指名專案，在 config.json projects[] 記載的 repo 路徑下背景啟動一個 claude -p session；不認得的專案名稱回報並跳過，不中斷整批。", "產生的 prompt 包含：該專案未 consumed 的 answer 記錄原文、一句明確的委派授權（允許依使用者的既有規則派 subagent）、以及「收工前用 brief-submit 投報告、被決策卡住時用 brief-submit 投問題」的指示。", "spawn 成功（process 啟動且取得 session id 或 log 位置）即 append 更新後的 answer 行把該專案取用的 answer 標為 consumed；spawn 失敗則不標，下次 dispatch 重試。不要求 session 內部回傳任何收訖確認。", "一次派兩個專案時其 session 併行執行；腳本 spawn 完即退出、不等 session 跑完，並逐專案印出 session id 或 log 位置。"], "touches": ["scripts/dispatch.py"], "requires": ["F1", "F2"], "name": "dispatch 腳本：派出 headless 接續 session"}
{"id": "F6", "status": "passing", "signed_off": true, "acceptance": ["README 記載 plugin 安裝指令；在沒有既有安裝的環境照著逐字執行後，新 session 中 /brief 與 /brief-init 可叫用，且此條以實際執行安裝來驗證。", "README 在第一個二級標題之前寫明：這是什麼（agent 的非同步決策收件匣）、每日迴圈（睡前審核、派工、睡覺、晨間審核）、日常唯一命令（/brief），以及兩個誠實限制：只支援 Claude Code（MCP server 版是未來工作）、過夜 session 需要機器保持喚醒。", "README 記載 collector 掛鉤（如何把任何來源的結構化待辦餵進收件匣）並附一個通用範例。", "隱私 grep 乾淨且只驗不修：對所有追蹤中的檔案搜尋 C:/Users/user、C:\\Users\\user、/Users/user、OneDrive、Obsidian、ian23x、lift-log、offer-radar、mission-control、instagram-place-to-maps、instagram-reels-summarizer、TDA-MDS 全部無命中（排除 feature_list.json 內作為搜尋詞的本清單自身）；任何其他命中即回報並阻斷本條，不靜默修掉。（2026-08-20 修正原因：原條文自指必然命中，經使用者重新簽核）", "skill 以 plugin 命名空間形式叫用（/agent-brief-me:brief 等）即滿足本 feature 與 F6 條 1 的「可叫用」；裸名 /brief 非要求。（2026-08-20 使用者確認）"], "touches": ["README.md", ".claude-plugin/plugin.json"], "requires": ["F2", "F3", "F4", "F5"], "name": "plugin 包裝與 README"}
{"id": "F7", "status": "passing", "signed_off": true, "acceptance": ["docs/headless-delegation-evidence.md 記錄 2026-08-20 既有觀察（session ba5dd2b3），不重跑任何東西：一個 headless claude -p session 成功派出 Explore subagent，證據為 (a) 逐字稿中有 subagent_type 為 Explore 的 Agent tool call、(b) modelUsage 出現 haiku 用量、(c) permission_denials 為空、(d) 最終輸出原封轉述 subagent 回報。", "文件說明如何稽核任何被派出的 session：逐字稿位於 ~/.claude/projects/<cwd-encoded>/<session_id>.jsonl，其中 <cwd-encoded> 是該 session 工作目錄絕對路徑、把路徑分隔符與磁碟冒號替換為連字號；不得以真實使用者路徑作為字面範例。"], "touches": ["docs/headless-delegation-evidence.md"], "requires": [], "name": "headless 委派證據文件"}
{"id": "F8", "status": "passing", "signed_off": true, "acceptance": ["`python scripts/brief_me.py serve [--port N]`（預設 8765）用 stdlib http.server 在 127.0.0.1 開 HTTP 服務；GET / 回傳 scripts/brief_me.html 的內容（Content-Type text/html; charset=utf-8），檔案不存在時回 500 並附 {ok:false,error}。", "API 契約（F8 實作、F9 消費，兩邊以此為準，不得各自擴充）：GET /api/state -> 不執行 collector（`collector_warning` 固定 null），其餘與 `brief_me.py load` 相同，另加 `projects`（config.json 專案名稱陣列）、`unconsumed_counts`（{專案名: 未消費答案數}）、`dismissed`（與 pending_questions 同形狀的 {專案: [question]}，只含折疊後現況為 cancelled 的問題，每筆多 `dismissed_at`＝最後一筆 cancelled 的 at）；POST /api/collect {} -> 執行 config.json 的 collector，回 {ok, collector_warning}；POST /api/read {ids:[...]} -> 對每個 id append read 狀態；POST /api/answer {answers:[{id, chosen?: string|string[], free_text?: string}], dismiss:[id...], dispatch?: {projects:[...], watch: bool}} -> 依序對每個 answer append 答案行與 answered 狀態、對 dismiss 的每個 id append cancelled 狀態，然後若有 dispatch 則以 `subprocess.run([sys.executable, scripts/dispatch.py, (--watch), *projects], capture_output=True, text=True)` 呼叫，逐行解析 stdout 中 `<project>: dispatched, log=<path>` 取得 log_paths（watch 時為空陣列）、`<project>: dispatched` 或 `<project>: opened in new window` 列入 dispatched；回傳 {ok:true, saved:n, dismissed:n, dispatched:[...], log_paths:[...], dispatch_error?: string}——dispatch.py 非零退出時仍回 200、`ok:true`、`dispatched` 只列成功者、`dispatch_error` 為 stdout+stderr 尾 2000 字，答案與 dismiss 的寫入不因 dispatch 失敗回滾；POST /api/reopen {ids:[...]} -> 對每個 id append {type:'status', ref:id, status:'reopened', at}；輸入驗證（全部在寫入前檢查，任一失敗回 400 {ok:false,error} 且兩個檔案位元組長度不變）：body 不是 JSON 物件；answers 某條 `chosen` 與 `free_text` 皆缺、或 `free_text` 去空白為空且無 `chosen`；同一 id 同時在 answers 與 dismiss；answers/dismiss/reopen/read 的任一 id 不存在於 inbox 的 question（read 為 report）；`multi` 與 `chosen` 型別不符不擋、原樣寫入。所有端點只綁 127.0.0.1、只接受 JSON、未知路徑 404、內部錯誤 500 加 {ok:false,error}。", "狀態折疊規則新增 `reopened`：對同一 ref，依檔案順序最後一筆 status 決定現況；`reopened` 之後該問題回到 pending（會再出現在 pending_questions、可再被回答或 dismiss；再次 dismiss 後再次出現在 dismissed）。docs/schema.md 的 status enum 加入 `reopened`、寫明此折疊規則，並把 Lifecycle 一節「answered/cancelled are terminal」改為「answered 為終態；cancelled 可經 reopened 回到 pending」，`rg -n terminal docs/schema.md` 不得殘留舊句。", "`load` 與 GET /api/state 的每個 question 帶 `multi`（有才帶）與 `options`（recommendation 優先，再 choices，不含 Dismiss/Skip——Dismiss/Skip 由前端自行呈現；CLI 的 build_option_list 維持原樣不受影響）。", "POST /api/answer 的 chosen 為陣列時原樣寫入 answers.jsonl；answers 與 dismiss 同時存在時先處理 answers 再 dismiss。", "tests/test_brief_me_api.py（stdlib unittest，BRIEF_HOME 指向暫存目錄、以隨機埠在背景執行緒啟動 serve）覆蓋：state 形狀含 projects/unconsumed_counts/dismissed；/api/state 連打三次不觸發 collector（collector 設為會建立 sentinel 檔的指令）而 /api/collect 會；answer 單選／多選／僅 free_text；dismiss 後出現在 dismissed 且 dismissed_at 正確；reopen 後回到 pending，再 dismiss 後再回 dismissed；400 案例各一（缺 chosen 與 free_text、同 id 衝突、未知 id）且檔案長度不變；dispatch 以 BRIEF_CLAUDE_CMD 指向假指令：headless 回 log_paths 非空、watch 回空陣列、含不存在專案時回 dispatch_error 且 dispatched 不含它且答案已寫入。`python tests/test_brief_me_api.py` 全綠。", "不改動 scripts/dispatch.py 與 skills/ 下任何檔案；不新增第三方依賴。"], "touches": ["scripts/brief_me.py", "docs/schema.md", "tests/test_brief_me_api.py"], "requires": [], "evidence": ["cmd: python tests/test_brief_me_api.py → Ran 15 tests OK (py 3.14 & 3.13)", "cmd: python tests/test_concurrent_append.py → OK 100 lines", "file: docs/schema.md", "note: acceptance-verifier 6/7 pass; R7 fail P3 = skills/brief-me/SKILL.md 連帶修正 (commit 7fef6fc, 主 session 非 worker) 判 out-of-scope REJECT，原因 R4 使 load 不再帶 Dismiss/Skip，skill 必須自行補上"], "name": "Web UI 後端：brief_me.py serve 與 reopened 狀態"}
{"id": "F9", "status": "passing", "signed_off": true, "acceptance": ["單一檔案 scripts/brief_me.html，純 HTML/CSS/JS、無外部依賴（字型可用 Google Fonts link 但要有系統字型 fallback），深色主題，所有資料來自 F8 的 API（契約原文見 F8 第 2 條），不含任何寫死的問題或報告資料。頁面載入與每次 Save/reopen 成功後呼叫 GET /api/state；標題列的 Refresh 鈕先 POST /api/collect 再 GET /api/state，並把 collector_warning（若有）以 toast 顯示。", "版面：左側欄＝專案列表（All projects＋config 專案；每項顯示 `未讀報告數 · 待答題數`；有 high 待答題顯示紅點、有任何待處理顯示綠點、無則灰）＋專案列表下方「Dismissed (n)」入口＋左下角 Answers 面板；右側＝標題列（目前專案名、`n questions · m unread reports`、Refresh 鈕）與內容區。", "內容區有 QUESTIONS 與 REPORTS 兩個可收放區塊，標題列顯示計數，收起時計數仍可見；沒有項目時顯示空狀態文字。", "問題卡片：severity 標籤（high 紅底）、專案名、標題、時間、body；第一行＝編號 1..n 的選項按鈕＋「Skip for now」；第二行＝「Other answer」按鈕＋常駐文字輸入框；第三行＝有 session_id 時左側顯示可點擊複製的 `claude --resume <session_id>`，右側紅字「Dismiss question」（hover 提示永久丟棄）。開啟時所有選項皆為未選取樣式，不預先高亮。", "單選題：選項、Other、Skip 三者互斥，再點同一個不取消（維持選取）。多選題（question.multi 為 true；標題旁顯示 `multi` 標籤）：選項與 Other 各有勾選框可複選，Skip 仍與全部互斥。任何題在輸入框打字即自動選取 Other（多選則加入集合），清空文字不取消 Other。 送出形狀：Other 一律只以 `free_text` 表達，`chosen` 永不含 Other 字樣；單選選了選項送 {id, chosen:'<選項原文>'}，只選 Other 送 {id, free_text}；多選送 {id, chosen:[...已勾選項原文...], free_text?}，只勾 Other 則省略 chosen。", "Other 驗證：輸入框 blur 時若 Other 被選取且文字為空，框線轉紅並顯示紅字提示；按 Save 時若任一題 Other 被選取且為空，擋下不送出並把那些題標紅。", "Dismiss question：卡片立即收成「Will be dismissed on save」＋Undo；按 Save 才真正送出 dismiss。Skip 不寫任何東西也不出現在送出 payload。reopened 後的題目與一般 pending 題目完全相同，可再次 Dismiss。", "Answers 面板：顯示未儲存數（有未儲存時為黃色）、各專案 `n new · m saved`（m 取自 /api/state 的 unconsumed_counts）；勾選「Dispatch after save」；「Open windows (--watch)」常駐顯示，未勾派工時為 disabled 樣式且不可點，取消派工時自動取消 watch；主按鈕文字依狀態為 `Save N answers` / `Save N and dispatch` / `Dispatch N projects`（無新選取但有 unconsumed_projects 且勾派工時）/ `Save answers`（disabled）。面板底部一句說明：選取不會立即寫入，儲存後的答案等待下一次派工。", "按 Save：若有題目完全未處置（未選、未 Skip、未 Dismiss），先彈出對話框列出那些題（專案＋標題）並提供「Skip them and save」與「Back」；確認後送 POST /api/answer（answers 依第 5 條形狀；dismiss 列表；有勾派工則帶 dispatch:{projects, watch}，其中 projects ＝ 本次送出答案所屬專案 ∪ /api/state 的 unconsumed_projects，與側欄目前選取無關，去重後依名稱排序），成功後重新載入 state 並以 toast 顯示結果：saved/dismissed 數、dispatched 專案、log_paths 或「opened windows」、dispatch_error（若有，以錯誤樣式）。", "報告卡片：預設折疊顯示專案、標題首行、時間、未讀綠點；點擊展開全文並立即 POST /api/read 標為已讀（綠點消失）；同一報告在本次頁面已標過讀就不再 POST；展開時顯示可複製的 `claude --resume <session_id>`。", "Dismissed 頁：由側欄入口切換，顯示 `dismissed` 清單，上方有專案下拉選單（All projects＋各專案）與標題關鍵字輸入框、右側顯示 `符合 n / 全部 m`；兩個篩選只作用於本頁，不改變側欄目前選取的專案；每筆有 Restore 鈕，按下後 POST /api/reopen 並立即切回收件匣視圖、該題出現在 QUESTIONS 可直接作答。在 Dismissed 頁點側欄任一專案即回到收件匣視圖。", "無障礙與回饋：所有可點元素 cursor pointer、鍵盤 focus 可見、按鈕高度至少 36px、hover/展開過渡 150ms、任何 API 失敗以 toast 顯示錯誤文字不靜默。", "驗證：tests/test_brief_me_html.py 使用 Python Playwright（以 `py -3.13 tests/test_brief_me_html.py` 執行，環境已裝 playwright 與 chromium；不得退回手動清單）。測試以環境變數 BRIEF_API_URL 指定後端；未設時自動啟動 tests/_stub_api.py（只實作 F8 第 2 條契約、append 到 BRIEF_HOME）。測試 inbox 含：單選題（有 recommendation）、多選題、high 題、一題已 cancelled、兩份報告（一份含 session_id）。斷言：選取單選→Save→answers.jsonl 出現 {chosen} 且無 free_text；多選勾兩項＋Other→{chosen:[2 項], free_text}；只選 Other→只有 free_text；Skip 的題不出現在 answers 也無 status；Dismiss→cancelled 狀態；Dismissed 頁 Restore→reopened 狀態且題目回到 QUESTIONS；未勾派工時 watch 控件 disabled；Save 時有未處置題目會出現確認框；展開報告→read 狀態只 append 一次。F9 改 passing 前，主 session 以 BRIEF_API_URL 指向真的 `brief_me.py serve` 再跑一次全綠並記錄於 docs/evidence/F9.md。"], "touches": ["scripts/brief_me.html", "tests/test_brief_me_html.py", "tests/_stub_api.py", "docs/evidence/F9.md"], "requires": ["F8"], "evidence": ["cmd: py -3.13 tests/test_brief_me_html.py → 9 scenes OK (stub)", "cmd: BRIEF_API_URL=http://127.0.0.1:18799 BRIEF_HOME=<tmp> py -3.13 tests/test_brief_me_html.py → 9 scenes OK (real serve, HEAD c2b8381)", "file: docs/evidence/F9.md", "note: acceptance-verifier 13/13 pass，另補 6 支暫存 Playwright 腳本覆蓋測試未及分支（dispatch union 用真 dispatch.py＋假 claude）"], "name": "Web UI 前端：scripts/brief_me.html 單檔收件匣介面"}
{"id": "F10", "status": "passing", "signed_off": true, "acceptance": ["README.md 與 README.zh-TW.md 各新增 `## Web UI` 章節，插在 `### Skills` 之後、`## Dispatch settings` 之前，內容涵蓋：啟動指令 `python ~/.claude/skills/agent-brief-me/scripts/brief_me.py serve`（可加 `--port`）、只綁 127.0.0.1、零 token、與 /brief-me 讀寫同兩個檔案可混用、Refresh 會跑 collector。", "同節以一張表說明四種處置的寫入語意：選項／Other＝寫 answers.jsonl 並標 answered；Skip＝不寫、下次再問；Dismiss＝寫 cancelled、在 Dismissed 頁可 Restore（寫 reopened）；Dispatch after save＝儲存後呼叫 dispatch.py。", "Features 清單新增一條 Web UI；mermaid 流程圖中 `A[\"/brief-me<br/>(before bed)\"]` 與 `F[\"/brief-me<br/>(morning)\"]` 兩個節點的文字都改為 `/brief-me or Web UI`（zh-TW 版對應節點同樣處理）。", "兩語版內容對應一致。私人資訊檢查：`rg -n -i \"RyanLeeYi|C:\\\\\\\\Users|OneDrive|ian23x\" README.md README.zh-TW.md` 只允許命中 GitHub clone URL `github.com/RyanLeeYi/agent-brief-me`，其餘為零。"], "touches": ["README.md", "README.zh-TW.md"], "requires": ["F8", "F9"], "evidence": ["cmd: grep -n -i -E 'RyanLeeYi|C:\\Users|OneDrive|ian23x' README.md README.zh-TW.md → 只命中兩處 clone URL", "file: README.md", "file: README.zh-TW.md", "note: acceptance-verifier 4/4 pass，兩語版標題行號完全對應"], "name": "Web UI 文件：README 兩語版加入 serve 用法與語意說明"}
{"id": "F11", "name": "Web UI 報告改為手動標已讀", "status": "passing", "signed_off": true, "touches": ["scripts/brief_me.html", "tests/test_brief_me_html.py", "docs/archive/features.jsonl"], "requires": ["F9"], "acceptance": ["取代 F9 第 6 條：點擊報告卡片只展開／收合全文，不 POST /api/read，綠點維持。", "展開後的全文區有一顆 `Mark read` 按鈕；按下才 POST /api/read（該報告本次頁面只 POST 一次），成功後綠點消失、按鈕變為 disabled 的 `Read`；失敗時 toast 錯誤且綠點與按鈕維持原狀。", "重新整理（Refresh 或重開頁面）後，未按 `Mark read` 的報告仍列在未讀區；按過的不再出現。", "tests/test_brief_me_html.py 的 acceptance #9 場景改為：展開後 status 行數為 0 → 按 `Mark read` 後為 1 → 再展開收合不增加；執行方式與 F9 相同（`py -3.13 tests/test_brief_me_html.py`）。"], "evidence": ["py -3.13 tests/test_brief_me_html.py 9 scenes OK (2026-08-22)", "acceptance-verifier 4/4 pass, git status clean before/after"]}
{"id": "F12", "name": "notify 放寬：report 與所有 question 都觸發，argv 帶 type 與 severity", "status": "passing", "signed_off": true, "touches": ["skills/brief-submit/SKILL.md", "README.md", "docs/schema.md", "tests/test_brief_submit_notify.py"], "requires": ["F3"], "acceptance": ["brief-submit 成功 append 任一筆 question 或 report 後，若 config.json 的 notify 非 null，恰呼叫一次 notify，argv 為 `<notify...> <project> <type> <severity> <text>`：type 屬 {question,report}；severity 為記錄的 severity，缺省時 question 填 normal、report 填 none；text 為 question 的 title 或 report summary 的第一行。", "notify 為 null 時不呼叫；notify 執行失敗不撤銷 append、回傳仍為成功並附 warning（維持 F3 既有語意）。", "README 與 skills/brief-submit/SKILL.md 的 notify 契約段改成上述 argv（英文），移除『只有 high 觸發』的描述；~/.claude/scripts/brief-notify.py 為使用者側範例，不在本 repo 內、不列驗收。", "tests/test_brief_submit_notify.py（stdlib only，`python tests/test_brief_submit_notify.py`）：以一支把 argv 寫進檔案的假 notify 腳本驗證——low question、high question、report 各呼叫一次且 argv 正確；notify 為 null 時檔案不存在；notify 指向不存在的程式時 append 仍成功。"], "evidence": ["python tests/test_brief_submit_notify.py OK (2026-08-22)", "acceptance-verifier 4/4 pass after R3 doc fix at 915d416"]}
{"id": "F13", "name": "派工記錄、側欄 running 狀態與整批完成通知", "status": "passing", "signed_off": true, "touches": ["scripts/dispatch.py", "scripts/brief_me.py", "scripts/brief_me.html", "docs/schema.md", "README.md", "tests/test_dispatch_batch.py", "tests/test_brief_me_html.py"], "requires": ["F8", "F9", "F12"], "acceptance": ["dispatch.py 每成功 spawn 一個 session 就往 ~/.agent-brief/dispatches.jsonl append 一行 {batch_id, project, pid, started_at, log}（append-only，docs/schema.md 補這個檔的定義；--watch 模式同樣記錄）。", "同一次 dispatch.py 執行的所有 session 共用一個 batch_id；dispatch.py 不阻塞等待，而是另起一個背景 waiter 行程，待該批所有 pid 結束後往 dispatches.jsonl append 一行 {batch_id, finished_at, exit_codes}，並在 config.json notify 非 null 時呼叫一次 notify，argv 為 `<notify...> <project 以逗號串接> batch none <N/N sessions finished>`。", "GET /api/state 多一個 `running` 欄位：{專案名: {started_at, elapsed_seconds}}，只含 dispatches.jsonl 裡已 started 且所屬 batch 尚未 finished、且 pid 仍存活的 session（pid 判活用 stdlib；判不出就視為存活）。", "Web UI 側欄：專案有 running 時在名稱旁顯示 `running <elapsed>`（格式沿用既有 age），Refresh 後消失條件同上；All projects 列不顯示。", "tests/test_dispatch_batch.py（stdlib only）：用 `python -c \"import time;time.sleep(1)\"` 代替 claude 指令跑 dispatch.py 兩個專案，驗證 dispatches.jsonl 先有兩行 started、waiter 結束後多一行 finished 且 exit_codes 全 0、假 notify 收到 `batch none 2/2 sessions finished`；tests/test_brief_me_html.py 加一個 scene：stub 回傳 running 時側欄出現 `running`。"], "evidence": ["python tests/test_dispatch_batch.py OK; py -3.13 tests/test_brief_me_html.py 10 scenes OK (2026-08-22)", "acceptance-verifier 5/5 pass at 7d96cb3"]}
{"id": "F14", "name": "Sessions 頁：派出的 session 在做哪些任務、跑多久、結果", "status": "passing", "signed_off": true, "touches": ["scripts/dispatch.py", "scripts/brief_me.py", "scripts/brief_me.html", "docs/schema.md", "tests/test_dispatch_batch.py", "tests/test_brief_me_api.py", "tests/test_brief_me_html.py"], "requires": ["F13"], "acceptance": ["dispatch.py 的 started 記錄多一個 `tasks` 欄位：該次派工 consumed 的每則 answer 對應的 question title 陣列（順序同 prompt），沒有就是空陣列；docs/schema.md 同步。舊記錄沒有此欄位時視為空陣列，不得 500。", "GET /api/state 多一個 `sessions` 欄位：{running: [...], finished: [...]}。running 元素＝{batch_id, project, pid, started_at, elapsed_seconds, tasks, log, current}，判定條件同既有 `running`（batch 無 finished 且 pid 存活）；`current` 是該專案 path 下 `.harness/current_feature` 的內容字串與檔案 mtime 的 ISO 時間，檔案不存在為 null。finished 元素＝{batch_id, project, pid, started_at, finished_at, duration_seconds, exit_code, tasks, log}，只含 finished_at 在最近 7 天內（UTC）的 batch，依 finished_at 新到舊排序；另回傳 `finished_hidden`（7 天前被略過的 session 數）。既有 `running` 欄位維持不變。", "Web UI 側欄：Dismissed 下方新增 `Sessions` 入口，右側顯示 `<N> running`（N=0 時不顯示數字）；專案列原本的 `running <elapsed>` 文字改為一顆綠點（不顯示時間）；All projects 列不變。", "Sessions 檢視分 Running／Finished 兩段。Running 每個 session 一張卡：專案名、pid、watch 或 log 標記、elapsed（沿用 elapsedString）、tasks 逐一顯示為晶片、current 一行（內容＋「<age> ago」，null 顯示 `no current_feature`）。Finished 每個 session 一列：專案名、tasks、exit code（非 0 用 danger 色）、duration、log 有路徑時顯示 `log` 文字（純顯示路徑即可，不必開檔）。段標題旁顯示 `older hidden: <finished_hidden>`（為 0 時不顯示）。沒有任何 session 時顯示 empty-state。Refresh 重抓。", "tests：test_dispatch_batch.py 驗證 started 記錄帶 tasks（含 consumed answer 時為其 title）；test_brief_me_api.py 用 fixture dispatches.jsonl（一筆 running、一筆 3 天前 finished、一筆 10 天前 finished）驗證 sessions.running／finished 各一筆、finished_hidden=1、舊記錄缺 tasks 不 500、current 在有無 `.harness/current_feature` 時的值；test_brief_me_html.py 加 scene：stub 回傳 sessions 時側欄出現 `Sessions` 與 `1 running`、專案列不含 `running ` 文字、Sessions 檢視出現 tasks 晶片與 `older hidden: 1`。"], "evidence": ["py -3.13 tests/test_dispatch_batch.py, test_brief_me_api.py (16), test_brief_me_html.py (11 scenes) OK (2026-08-22)", "acceptance-verifier 5/5 pass at 43d01cc"]}
{"id": "F15", "name": "Sessions：以 report 判定完成，不以 pid 存活", "status": "passing", "signed_off": true, "touches": ["scripts/brief_me.py", "scripts/brief_me.html", "docs/schema.md", "tests/test_brief_me_api.py", "tests/test_brief_me_html.py", "tests/_stub_api.py"], "requires": ["F14"], "acceptance": ["GET /api/state 的 sessions.running 判定改為：該 session 的 batch 無 finished 記錄、pid 存活、**且** inbox.jsonl 中不存在「同 project 且 created_at ≥ 該 session started_at」的 report。三者任一不成立就不算 running。", "滿足「pid 存活、batch 無 finished、但已有符合上條的 report」的 session 歸入 sessions.finished，元素多一個 `ended_by` 欄位：值為 `\"report\"`；finished_at 取該 report 的 created_at（多筆取最早一筆），duration_seconds 據此計算，exit_code 為 null。原本由 waiter finished 記錄結案的元素 `ended_by` 為 `\"exit\"`。7 天窗口與排序規則不變。既有頂層 `running` 欄位套用同一判定（report 結案的專案不再顯示 running）。", "Web UI Finished 列：`ended_by` 為 report 時顯示 badge `REPORT` 並省略 exit code；為 exit 時維持現狀。Running 卡片不變。", "docs/schema.md 的 sessions 段落補一句：running 的結案來源有 waiter finished 與 report 兩種，並說明 report 比對規則。", "tests：test_brief_me_api.py 加 case——fixture 一筆 pid 存活（os.getpid）無 finished 的 started 記錄，inbox 有一筆同 project、created_at 晚於 started_at 的 report → 該 session 在 finished 且 ended_by=report、exit_code=null、頂層 running 不含該專案；另一筆 report created_at 早於 started_at → 仍在 running。test_brief_me_html.py 加 scene：stub finished 元素帶 ended_by=report 時列上出現 `REPORT` 且不含 `exit`。"], "evidence": ["py -3.13 tests/test_brief_me_api.py (17), test_brief_me_html.py (12 scenes) OK (2026-08-22)", "acceptance-verifier 5/5 pass at 2c66c53"]}
{"id": "F16", "name": "--watch 派工前預先 trust 專案目錄，避免互動視窗卡在 trust 對話框", "status": "passing", "signed_off": true, "touches": ["scripts/dispatch.py", "tests/test_dispatch_trust.py", "docs/schema.md"], "requires": [], "acceptance": ["新增 ensure_trusted(claude_json_path, project_path)：讀 claude.json，若 projects[<path>].hasTrustDialogAccepted 不為 true 則寫成 true 並回傳 True；已是 true 回傳 False 且檔案內容不變。path 的 key 格式與 Claude Code 既有寫法一致（正斜線、無尾斜線，例 C:/Users/user/OneDrive/Desktop/SideProject/lift-log）；projects 缺該 key 時新建條目。", "寫入用 tempfile + os.replace 原子取代，保留其他 project 與頂層欄位一字不變（測試：先放含兩個 project 與頂層雜項欄位的 fixture，跑完 diff 只多一個 flag）。", "只在 --watch 模式呼叫；headless -p 不碰 claude.json。claude.json 路徑預設 ~/.claude.json，可由 BRIEF_CLAUDE_JSON 覆寫（測試用）。", "claude.json 不存在、非合法 JSON、或寫入失敗 → 印一行 warning 到 stderr 並照常派工（fail-open），不得讓整批派工中止。", "派工 stdout 在有改寫時多印一行 `<project>: trusted <path>`，方便事後知道是這次派工幫它 trust 的。", "回歸測試至少四條：未 trust→寫入、已 trust→不動、缺檔→warning 且派工繼續、其他 project 欄位保留。python -m unittest 全綠。"], "evidence": "acceptance-verifier 2026-08-22: 6/6 pass; tests/test_dispatch_trust.py 4/4; discover -s tests 22 pass (test_brief_me_html playwright ImportError pre-existing)"}
{"id": "F17", "name": "UI 的 watch 勾選框要能覆蓋 config 預設，不勾就不開視窗", "status": "passing", "signed_off": true, "touches": ["scripts/dispatch.py", "scripts/brief_me.py", "tests/test_dispatch_trust.py", "docs/schema.md"], "requires": ["F16"], "acceptance": ["dispatch.py 新增 --no-watch 旗標；解析規則：--watch → 開視窗；--no-watch → headless；兩者都沒給 → 沿用 config.json dispatch.watch。兩者同時給 → usage 錯誤 exit 2。", "brief_me.py 的 _run_dispatch 依 POST body 的 dispatch.watch 擇一附加 --watch 或 --no-watch（body 有 watch 欄位時一律明確傳，不再依賴 config 預設）。", "回歸測試：config watch=true 且 --no-watch → 走 headless（dispatches.jsonl 該筆 log 不為 null、claude.json 不被改寫）；config watch=false 且 --watch → 開視窗（log 為 null）；同時給兩旗標 → exit 2。", "docs/schema.md 的 dispatch API 說明補一句：watch 欄位為明確覆蓋，config 的 dispatch.watch 只在 CLI 未指定時生效。", "python -m unittest discover -s tests 除既有 playwright ImportError 外全綠。"], "evidence": "acceptance-verifier 2026-08-23: 5/5 pass; WatchOverrideTests 3/3; discover -s tests 24 pass (test_brief_me_html playwright ImportError pre-existing)"}
{"id": "F18", "name": "brief-scan 不重複投遞已回答但尚未消化的 sign-off question", "status": "passing", "signed_off": true, "touches": ["~/.claude/scripts/brief-scan.py", "~/.claude/scripts/brief-scan-test.py", "docs/schema.md"], "requires": [], "acceptance": ["brief-scan 對每個 (project, title) 的去重改為：存在同標題的 question 且 (a) 無 status，或 (b) 最新 status 為 answered 且 answers.jsonl 折疊後該 question 的答案 consumed 不為 true → 視為已有，跳過不投。只有「從沒問過」「已 cancelled」「已回答且答案已被派工消化」三種情況才投新的。", "去重結果在 stdout 摘要分開計數：`N filed, M pending, K answered-unconsumed`。", "回歸測試 ~/.claude/scripts/brief-scan-test.py（stdlib unittest，暫存目錄當 BRIEF_HOME）：四條——無任何 question → 投；同標題 pending → 不投；同標題 answered 且 unconsumed → 不投；同標題 answered 且 consumed、feature 仍 failing 未簽核 → 投。", "既有 --dry-run 行為不變。", "docs/schema.md 的 collector 說明補一句去重規則。"], "evidence": "acceptance-verifier 2026-08-23: 5/5 pass; brief-scan-test.py 5/5; real inbox dry-run files no sign-off dup (test_brief_me_html playwright ImportError pre-existing)"}
{"id": "F19", "name": "Report 卡片：三段結構化摘要（Done / Blocked / Handoff）", "status": "passing", "signed_off": true, "touches": ["scripts/brief_me.py", "scripts/brief_me.html", "tests/test_brief_me_api.py", "tests/test_brief_me_html.py"], "requires": [], "acceptance": ["report_view() 新增 `parts` 欄位：summary 以全形「；」或半形「;」切成最多三段，依序對應 done / blocked / handoff（缺段為 null）；切不到任何分隔符時 parts 全為 null，summary 原樣保留。", "report_view() 新增 `feature_chip`：從 done 段（無 done 段則整段 summary）以 regex 抓出 `F\\d+` 序列與終態關鍵字（passing / failing / blocked），輸出如 `F11-F15 passing`（連號壓成範圍）或 `F10 F22 blocked`；抓不到 → null。", "brief_me.html 的 report 卡片收合列改為：[未讀點] [project 固定寬 150px] [feature_chip 圓角 chip，passing=accent 配色、blocked=warning 配色、null 時顯示灰色 `no change`] [blocked 段首行，單行省略] [age 右對齊] [chevron]。", "展開區改為 grid 三列（標籤欄 72px）：Done / Blocked / Handoff，標籤 11px 大寫，Blocked 標籤用 --warning 色；parts 全 null 時退回現有的 summary 全文 <p>。下方一列：resume 指令（現有 .resume-cmd）+ `Mark read` 按鈕靠右。", "色值與字型一律用既有 :root 變數與 IBM Plex，不新增 hex 常數。", "tests/test_brief_me_api.py 新增：三段 summary 解析為 parts 與 feature_chip；無分隔符 → parts 全 null、chip 為 null。tests/test_brief_me_html.py 新增：/api/state 含三段 report 時 DOM 出現 Done/Blocked/Handoff 三個標籤且 chip 文字正確（沿用該檔現有的 DOM 測法；playwright 缺席時照既有 skip 慣例）。", "既有測試全綠：`python -m unittest discover tests`。"], "evidence": ["acceptance-verifier 2026-08-23: 7/7 pass (HEAD 3100428)"]}
{"id": "F20", "name": "Sessions 視圖：Finished 表格化＋Outcome 翻譯＋Running 進度", "status": "passing", "signed_off": true, "touches": ["scripts/brief_me.py", "scripts/brief_me.html", "tests/test_brief_me_api.py", "tests/test_brief_me_html.py"], "requires": [], "acceptance": ["sessions_view() 的 finished 每筆新增 `outcome`：ended_by=report → `report`；exit_code 0 → `ok`；exit_code 為 4294967295 或 -1 → `killed`；3221225786 → `ctrl_c`；其他非 0 → `exit`；None → `unknown`。另新增 `report_id`：finished_at 之後（或同一 project 在 started_at 之後）最近一則該 project 的 report id，沒有則 null；有 report_id 的列 outcome 仍依上述規則（Ctrl-C 且有 report 就是 ctrl_c + report_id）。", "sessions_view() 回傳新增 `finished_counts: {report, killed}`（killed 含 ctrl_c）。", "brief_me.html Finished 區改為表格：表頭 Outcome / Project / Tasks / Took / Ended / Next（11px 大寫 muted）；grid 欄寬 92px 150px 1fr 60px 50px 140px。Outcome 欄顯示 badge：report=accent 配色帶勾 SVG、killed=danger 配色帶叉 SVG、ctrl_c=warning 配色帶驚嘆 SVG、exit=灰 badge `Exit N`、ok=灰 `Exit 0`；killed / ctrl_c 的列整列背景 rgba(248,81,73,0.04)。", "Tasks 欄：feature id 壓成一顆 mono chip（連號壓範圍、超過 3 個顯示 `+N`）＋ 副文：有 report_id → 該 report 的 blocked 段或 summary 首行；killed 無 report → danger 色 `no report`。Next 欄：有 report_id → `open report` 連結（點了切到 inbox 並展開該 report）；killed 無 report → `--resume <session_id>` mono chip（session_id 從 log 檔名或 dispatches 取不到時顯示 `open log`）；其餘有 log → `open log`。", "Finished 區標題右側顯示 `N reported · M killed · older hidden: K`（用 finished_counts）。", "Running 卡片：左邊 3px accent 邊線；tasks chip 中與 current.feature 相同者用 accent 配色；底部一列：進度條 120x4px（passing 數 / tasks 數，passing 數由 _project_current 同目錄的 feature_list.json 算，取不到則不顯示進度條）、`now F14 · 2m ago`、`last tool <mono>`（last tool 從 .harness/trace.jsonl 最後一筆 tool 名取，取不到不顯示）。", "色值與字型一律用既有 :root 變數與 IBM Plex，不新增 hex 常數（列背景那個 rgba 以 --danger 派生的新變數 --danger-bg 定義在 :root）。", "tests/test_brief_me_api.py 新增：exit_codes 含 4294967295 / 3221225786 / 0 / 2 四種各對應 outcome；有 report 的 batch 帶 report_id；finished_counts 正確。tests/test_brief_me_html.py 新增：Finished 表格渲染出 Killed 與 Report badge 文字（沿用現有 DOM 測法／skip 慣例）。", "既有測試全綠：`python -m unittest discover tests`。"], "evidence": ["acceptance-verifier 2026-08-23: 8/9, P3 Exit-badge casing fixed in follow-up commit; 15/15 Playwright scenes + api tests green"]}
{"id": "F21", "name": "手機版面：sessions 表格與 report 卡片在 390px 不跑版", "status": "passing", "signed_off": true, "touches": ["scripts/brief_me.html", "tests/test_brief_me_html.py"], "acceptance": ["`@media (max-width:768px)` 內：`.session-table-head` 隱藏；`.session-row` 改 `grid-template-columns:auto 1fr` 兩欄，Outcome 佔第一欄、Project+Tasks 佔第二欄首列、Took/Ended/Next 合併成第二列（font-size 12px、muted），列不得橫向溢出。", "同 media 內：`.report-card .card-project` 改 `width:auto; max-width:40%`；`.report-summary-row` 允許 `flex-wrap:wrap`，feature chip 與 blocked 首行掉到第二列；age 仍靠右。", "390px 寬度下 `document.documentElement.scrollWidth <= 390`（inbox 與 sessions 兩個 view 都成立）。", "tests/test_brief_me_html.py 新增 scene：`page.set_viewport_size(390x844)` 載入 F20 fixture，斷言第 3 條，並在 scene 結束把 viewport 還原。", "色值／字型只用既有變數；既有 15 scene 全綠。"], "evidence": ["acceptance-verifier 2026-08-23: 5/5 pass (HEAD 0ab9af8); 16/16 Playwright scenes"]}
